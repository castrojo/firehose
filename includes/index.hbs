<!DOCTYPE html>
<html lang="en">

  <head>
    <title>The Firehose - Cloud Native Releases</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="robots" content="noindex, nofollow" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="shortcut icon" type="image/x-icon" href="favicon.ico" />
    <link rel="alternate" type="application/rss+xml" title="{{siteTitle}}" href="{{feedHref}}" />
    <link href="index.css" rel="stylesheet" />
    <!-- %before-head-end.html% -->
  </head>

  <body>
    <!-- %after-body-begin.html% -->
    <div class="container">
      <header class="site-header">
        <h1 class="site-title">The Firehose</h1>
        <p class="site-subtitle">Cloud Native & CNCF Project Releases</p>
      </header>

      <main class="releases-container">
        {{#each dates}}
          <section class="date-section">
            <h2 class="date-heading">
              <time datetime="{{isoUtcPublishTime}}">{{isoOffsetPublishDate}}</time>
            </h2>
            
            {{#each sources}}
              <div class="source-group">
                <h3 class="source-title">
                  <a href="{{siteUrl}}" class="source-link">{{title}}</a>
                </h3>
                
                {{#each articles}}
                  <article class="release-card">
                    <div class="release-header">
                      <h4 class="release-title">
                        <a href="{{link}}" target="_blank" rel="noopener noreferrer">{{title}}</a>
                      </h4>
                      <time class="release-time" datetime="{{publishedOn}}">{{isoOffsetPublishDate}}</time>
                    </div>
                    <div class="release-body markdown-body">
                      {{#if htmlContent}}
                        {{{htmlContent}}}
                      {{else}}
                        {{description}}
                      {{/if}}
                    </div>
                    <div class="release-footer">
                      <a href="{{link}}" class="release-link" target="_blank" rel="noopener noreferrer">
                        View full release →
                      </a>
                    </div>
                  </article>
                {{/each}}
              </div>
            {{/each}}
          </section>
        {{/each}}
      </main>

      <footer class="site-footer">
        <p>
          {{#if githubRunUrl}}
            Last updated: <a href="{{githubRunUrl}}"><time datetime="{{siteBuildTimestamp}}">{{siteBuildTimestamp}}</time></a>
          {{else}}
            Last updated: <time datetime="{{siteBuildTimestamp}}">{{siteBuildTimestamp}}</time>
          {{/if}}
          | Powered by <a href="{{projectUrl}}">osmosfeed {{cliVersion}}</a>
        </p>
      </footer>
    </div>
    <script src="html-loader.js"></script>
    <!-- %before-body-end.html% -->
  </body>

</html>